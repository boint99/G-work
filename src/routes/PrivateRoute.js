// import { Navigate } from "react-router-dom";

// export default function PrivateRoute({ children }) {
//   const isLoggedIn = localStorage.getItem("token"); // ví dụ check token
//   return isLoggedIn ? children : <Navigate to="/login" replace />;
// }
